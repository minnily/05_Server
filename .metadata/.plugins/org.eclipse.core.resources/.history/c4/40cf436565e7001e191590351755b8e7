<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title><%= request.getParameter("sinnerList") %>의 곡 선택</title>
</head>
<body>

	<%-- [JSP용 주석!] --%>
	<%--
	 
	✔ <%@ %> : 지시자 태그 (▷ JSP 페이지의 전반적 속성을 설정하는데 사용하는 태그)
	✔ <% %>  : 스크립틀릿 ▷ 자바코드 작성
	✔ <%= %> : JSP 표현식 ▷ 자바, 서버에서 받아온 값을 표현(출력)할때 사용
	✔ JSP (Java Server Page) ? Java코드가 들어가있는 HTML 코드

	--%>
	
	<h1 style="color : hotpink"> <% request.getParameter("singerList")%> 의 어떤 곡을 고르시겠습니까?</h1>

   <br>
   <hr>
	
	<% if(request.getParameter("singerList") == null) { %>
	    <h1 style="color: red">Error</h1>
		<img src="/error.jpg">   
	<% } %> 
	<% if(request.getParameter("singerList")==request.getParameter("1.songList")) { %> 
		<%= request.getParameter("/song1")%>
	<% } %>
	<% if(request.getParameter("singerList")==request.getParameter("2.songList")) { %> 
		<%= request.getParameter("/song2")%>
	<% } %>
	<% if(request.getParameter("singerList")==request.getParameter("3.songList")) { %> 
		<%= request.getParameter("/song3")%>
	<% } %>
</body>
</html>